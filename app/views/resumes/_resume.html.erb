  <div class="col-md-3">
    <div class="thumbnail">
      <%= image_tag resume.attachment.expiring_url(3600, :thumb), class: "img-responsive" %>
      <div class="caption">
        <h3><%= resume.name %></h3>
        <p>
          <%= link_to 'Download', resume.attachment.expiring_url, class: "btn btn-primary" %>
        </p>
      </div>
    </div>
  </div>
